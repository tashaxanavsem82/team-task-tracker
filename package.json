{
  "name": "team-task-tracker",
  "version": "1.0.0",
  "description": "A lightweight task management tool designed for small teams to collaborate on projects effectively.",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently 'npm run client' 'npm run start'",  // run both client and server in development
    "lint": "eslint ."  // run ESLint to check code quality
  }
}